{
  "name": "ai-project-template",
  "version": "0.3.2",
  "description": "Bun standalone executable CLI template",
  "type": "module",
  "private": true,
  "module": "src/index.ts",
  "repository": "yigegongjiang/ai-project-template",
  "scripts": {
    "start": "bun run src/index.ts",
    "build": "bun run build.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.3"
  }
}

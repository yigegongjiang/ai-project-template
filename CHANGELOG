# Changelog (developer)

面向开发者，镜像 [CHANGELOG.md](./CHANGELOG.md) 每条变更并补技术细节。规则见 [llm-doc-style.md](./llm-doc-style.md)。

## [0.2.4] - 2026-06-08

### Changed

- README 精简: 补可执行命令名说明, 移除 Development/Release 章节
  - README + README.zh.md 同步: 新增 `## Usage`/`## 用法` 段点明命令名 = `package.json#name` = repo basename = `cli-template` (install.sh `BIN_NAME=${REPO##*/}` 约定, 装入 `~/.local/bin/`); 删除面向人的 Development/Release 章节, dev/release 流程由 `package.json` scripts + `deploy.md` 承载
- 构建清理 dist/ 多余 sourcemap (`*.js.map`), 运行时堆栈仍内嵌二进制
  - `build.ts` `cleanLeftovers()` 增扫 `./dist` 删 `*.map`: `--compile` 下 sourcemap 非 none 时 map 既 zstd 内嵌二进制 (运行时堆栈) 又外吐 `index.js.map` (install.sh 不分发, 纯残留); sourcemap 保持 `linked` (Bun 官方 `--compile --minify --sourcemap` 推荐, inline 在 compile 下等价)

## [0.2.3] - 2026-06-02

### Changed

- 跟随版本同步发布
  - 文档规范借鉴 elepay 范本: llm-doc-style 中英重写 (补「代码块」段 + 通用风格 3 条), 清除 setup.md/wrangler/Shiki/子项目等母模板残留, 硬约束改按需; deploy.zh 补 CHANGELOG 双文件镜像步骤; AGENTS 首段补 llm-doc-style link

## [0.2.2] - 2026-06-02

### Changed

- 跟随版本同步发布
  - 建立 AGENTS/README/deploy/llm-doc-style 文档分层 + 中文版 .zh.md + CHANGELOG 双文件机制 (用户向 + 开发向); CLAUDE.md 改为软链 AGENTS.md

## [0.2.1] - 2026-05-17

### Changed

- `update` 下载时显示进度条
  - 新增 `src/download.ts` `downloadWithProgress()`: 流式 `ReadableStream.getReader()` 累积分块, TTY 下 `\r` 重绘 + 100ms 节流, 非 TTY 降级为末尾单行; `fetchOk` 由 `index.ts` 迁出至此
- `update` 完成后打印更新前后的版本
  - `update()` 下载前打印 before; 替换后 `Bun.spawnSync([dest, "version"])` 取 after, best-effort (新二进制无法 exec 不影响已完成的替换)

## [0.2.0] - 2026-05-17

### Added

- 新增 `uninstall` 子命令: 卸载已安装的二进制 (仅编译后二进制可用)
  - `uninstall()`: `process.execPath` 定位自身, `basename(dest) !== NAME` 守卫拒绝源码 (bun run) 运行时卸载, `unlink` 删除; `main()` switch 增 `uninstall` case

## [0.1.0] - 2026-05-17

### Added

- 子命令 `help` / `version` / `update`
  - `src/index.ts` `main()` switch 路由各子命令; `update()` = 下载 + SHA256 校验 + `rename` 原子替换 + `chmod`; `BUILD_NAME`/`BUILD_VERSION`/`BUILD_REPO` 经 `build.ts` `--define` 编译期注入
- 支持 macOS x64 / arm64
  - `detectAsset()` 按 `process.platform` / `process.arch` 选产物名 (非 darwin 抛错); `build.ts` `Bun.build({ compile })` 交叉编译 `darwin-x64` / `darwin-arm64`, minify + linked sourcemap
- `install.sh` 一键安装
  - `install.sh`: `REPO`/`VERSION`/`INSTALL_DIR`/`BIN_NAME` 环境变量可配; 检测 OS+arch → 下载产物 → `shasum` 比对 `checksums.txt` → `mktemp` + `trap` 清理 → `chmod +x` + `mv -f` 装入 `~/.local/bin`; PATH 缺失则提示
- 自动发布, 产物附 SHA256 校验和
  - `.github/workflows/release.yml`: `v*` tag 触发 → 校验 tag 与 `package.json#version` 一致 → typecheck + build → `sha256sum` 生成 `checksums.txt` → `softprops/action-gh-release` (`generate_release_notes`) 发布 `dist/*`

[0.2.4]: https://github.com/yigegongjiang/cli-template/compare/v0.2.3...v0.2.4
[0.2.3]: https://github.com/yigegongjiang/cli-template/compare/v0.2.2...v0.2.3
[0.2.2]: https://github.com/yigegongjiang/cli-template/compare/v0.2.1...v0.2.2
[0.2.1]: https://github.com/yigegongjiang/cli-template/compare/v0.2.0...v0.2.1
[0.2.0]: https://github.com/yigegongjiang/cli-template/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/yigegongjiang/cli-template/releases/tag/v0.1.0
